# 第八章：全局类型定义与架构契约

## 8.1 全局DTO类型定义

### 8.1.1 统一响应格式

```typescript
// types/global.d.ts
declare namespace IntelligentHub {
  // 基础响应接口
  interface BaseResponse<T = any> {
    code: number;
    message: string;
    data: T;
    timestamp: number;
    requestId: string;
    success: boolean;
  }

  // 分页响应接口
  interface PaginatedResponse<T = any> extends BaseResponse<PageResult<T>> {}

  interface PageResult<T = any> {
    items: T[];
    total: number;
    page: number;
    pageSize: number;
    totalPages: number;
    hasNext: boolean;
    hasPrevious: boolean;
  }

  // 错误响应接口
  interface ErrorResponse {
    code: number;
    message: string;
    details?: string;
    path: string;
    timestamp: number;
    requestId: string;
    errors?: ValidationError[];
  }

  // 验证错误接口
  interface ValidationError {
    field: string;
    message: string;
    code: string;
    rejectedValue?: any;
  }

  // 排序参数
  interface SortOrder {
    field: string;
    direction: 'ASC' | 'DESC';
  }

  // 分页查询参数
  interface PageQuery {
    page?: number;
    pageSize?: number;
    sort?: SortOrder[];
    keyword?: string;
  }

  // 时间范围查询
  interface TimeRangeQuery {
    startTime?: Date | string;
    endTime?: Date | string;
  }

  // 批量操作响应
  interface BatchResponse<T = any> {
    success: number;
    failed: number;
    total: number;
    results: Array<{
      id: string;
      success: boolean;
      message?: string;
      data?: T;
    }>;
  }
}

```text

### 8.1.2 请求参数验证类型

```typescript
// types/validation.d.ts
declare namespace IntelligentHub {
  // 基础验证规则
  interface BaseValidation {
    required?: boolean;
    minLength?: number;
    maxLength?: number;
    pattern?: RegExp;
    customValidator?: (value: any) => boolean;
  }

  // 数字验证
  interface NumberValidation extends BaseValidation {
    min?: number;
    max?: number;
    integer?: boolean;
    positive?: boolean;
  }

  // 日期验证
  interface DateValidation extends BaseValidation {
    minDate?: Date;
    maxDate?: Date;
    futureOnly?: boolean;
    pastOnly?: boolean;
  }

  // 文件验证
  interface FileValidation extends BaseValidation {
    maxSize?: number; // bytes
    allowedTypes?: string[];
    allowedExtensions?: string[];
  }

  // 业务验证规则
  interface BusinessValidation {
    unique?: {
      table: string;
      column: string;
      excludeId?: string;
    };
    exists?: {
      table: string;
      column: string;
    };
    complexRule?: string;
  }
}

```text

## 8.2 领域模型类型定义

### 8.2.1 核心实体类定义

```java
// 基础实体接口
public interface BaseEntity<T> {
    T getId();
    void setId(T id);
    Date getCreatedAt();
    void setCreatedAt(Date createdAt);
    Date getUpdatedAt();
    void setUpdatedAt(Date updatedAt);
    String getCreatedBy();
    void setCreatedBy(String createdBy);
    String getUpdatedBy();
    void setUpdatedBy(String updatedBy);
    Integer getVersion();
    void setVersion(Integer version);
}

// 聚合根标记接口
public interface AggregateRoot extends BaseEntity<String> {
    Collection<DomainEvent> getDomainEvents();
    void clearDomainEvents();
}

// 值对象标记接口
public interface ValueObject {
    boolean equals(Object obj);
    int hashCode();
    String toString();
}

// 领域事件接口
public interface DomainEvent {
    String getEventId();
    Date getOccurredOn();
    String getEventType();
    String getAggregateId();
    String getAggregateType();
}

// 枚举类型定义
public enum EntityStatus {
    ACTIVE("active", "活跃"),
    INACTIVE("inactive", "非活跃"),
    DELETED("deleted", "已删除"),
    ARCHIVED("archived", "已归档");

    private final String code;
    private final String description;

    EntityStatus(String code, String description) {
        this.code = code;
        this.description = description;
    }

    // getters...
}

public enum OperationType {
    CREATE("create", "创建"),
    UPDATE("update", "更新"),
    DELETE("delete", "删除"),
    QUERY("query", "查询"),
    EXPORT("export", "导出"),
    IMPORT("import", "导入");

    private final String code;
    private final String description;

    OperationType(String code, String description) {
        this.code = code;
        this.description = description;
    }

    // getters...
}

```text

### 8.2.2 用户领域模型

```java
// 用户聚合根
public class User implements AggregateRoot {
    private String id;
    private String username;
    private String email;
    private String phone;
    private String password;
    private UserProfile profile;
    private UserStatus status;
    private Set<Role> roles;
    private Set<Permission> permissions;
    private Date lastLoginAt;
    private Integer loginCount;
    private Date createdAt;
    private Date updatedAt;
    private String createdBy;
    private String updatedBy;
    private Integer version;
    private final transient List<DomainEvent> domainEvents = new ArrayList<>();

    // 业务方法
    public void changePassword(String oldPassword, String newPassword) {
        // 密码变更逻辑
        this.registerEvent(new UserPasswordChangedEvent(this.id, newPassword));
    }

    public void assignRole(Role role) {
        this.roles.add(role);
        this.registerEvent(new UserRoleAssignedEvent(this.id, role.getId()));
    }

    public boolean hasPermission(String permissionCode) {
        return this.permissions.stream()
                .anyMatch(p -> p.getCode().equals(permissionCode));
    }

    // 聚合根方法
    @Override
    public Collection<DomainEvent> getDomainEvents() {
        return Collections.unmodifiableList(domainEvents);
    }

    @Override
    public void clearDomainEvents() {
        domainEvents.clear();
    }

    protected void registerEvent(DomainEvent event) {
        domainEvents.add(event);
    }

    // getters and setters...
}

// 用户档案值对象
public class UserProfile implements ValueObject {
    private final String firstName;
    private final String lastName;
    private final String displayName;
    private final String avatar;
    private final Gender gender;
    private final Date birthDate;
    private final String timezone;
    private final String language;

    public UserProfile(String firstName, String lastName, String displayName, 
                      String avatar, Gender gender, Date birthDate, 
                      String timezone, String language) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.displayName = displayName;
        this.avatar = avatar;
        this.gender = gender;
        this.birthDate = birthDate;
        this.timezone = timezone;
        this.language = language;
    }

    // 值对象相等性比较
    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof UserProfile)) return false;
        UserProfile that = (UserProfile) o;
        return Objects.equals(firstName, that.firstName) &&
               Objects.equals(lastName, that.lastName) &&
               Objects.equals(displayName, that.displayName) &&
               Objects.equals(avatar, that.avatar) &&
               gender == that.gender &&
               Objects.equals(birthDate, that.birthDate) &&
               Objects.equals(timezone, that.timezone) &&
               Objects.equals(language, that.language);
    }

    @Override
    public int hashCode() {
        return Objects.hash(firstName, lastName, displayName, avatar, 
                           gender, birthDate, timezone, language);
    }
}

// 用户状态枚举
public enum UserStatus {
    PENDING_ACTIVATION("pending_activation", "待激活"),
    ACTIVE("active", "活跃"),
    INACTIVE("inactive", "非活跃"),
    LOCKED("locked", "锁定"),
    SUSPENDED("suspended", "暂停");

    private final String code;
    private final String description;

    UserStatus(String code, String description) {
        this.code = code;
        this.description = description;
    }
}

// 性别枚举
public enum Gender {
    MALE("male", "男"),
    FEMALE("female", "女"),
    UNKNOWN("unknown", "未知");

    private final String code;
    private final String description;

    Gender(String code, String description) {
        this.code = code;
        this.description = description;
    }
}

```text

### 8.2.3 AI能力领域模型

```java
// AI模型聚合根
public class AIModel implements AggregateRoot {
    private String id;
    private String name;
    private String description;
    private ModelType type;
    private ModelProvider provider;
    private String modelId;
    private String version;
    private Map<String, Object> parameters;
    private Map<String, Object> capabilities;
    private ModelStatus status;
    private Double costPerRequest;
    private Integer maxTokens;
    private Integer rateLimit;
    private Boolean isDefault;
    private Date createdAt;
    private Date updatedAt;
    private String createdBy;
    private String updatedBy;
    private Integer version;
    private final transient List<DomainEvent> domainEvents = new ArrayList<>();

    // 业务方法
    public boolean supportsCapability(String capability) {
        return this.capabilities.containsKey(capability);
    }

    public boolean isAvailable() {
        return this.status == ModelStatus.ACTIVE;
    }

    public void updateParameters(Map<String, Object> newParameters) {
        this.parameters.putAll(newParameters);
        this.registerEvent(new ModelParametersUpdatedEvent(this.id, newParameters));
    }

    // getters and setters...
}

// 模型类型枚举
public enum ModelType {
    TEXT_GENERATION("text_generation", "文本生成"),
    TEXT_EMBEDDING("text_embedding", "文本嵌入"),
    IMAGE_GENERATION("image_generation", "图像生成"),
    CODE_GENERATION("code_generation", "代码生成"),
    SPEECH_RECOGNITION("speech_recognition", "语音识别"),
    TRANSLATION("translation", "翻译");

    private final String code;
    private final String description;

    ModelType(String code, String description) {
        this.code = code;
        this.description = description;
    }
}

// 模型提供商枚举
public enum ModelProvider {
    OPENAI("openai", "OpenAI"),
    ANTHROPIC("anthropic", "Anthropic"),
    GOOGLE("google", "Google"),
    AZURE("azure", "Azure OpenAI"),
    BEDROCK("bedrock", "AWS Bedrock"),
    LOCAL("local", "本地模型");

    private final String code;
    private final String description;

    ModelProvider(String code, String description) {
        this.code = code;
        this.description = description;
    }
}

// 模型状态枚举
public enum ModelStatus {
    DEVELOPMENT("development", "开发中"),
    TESTING("testing", "测试中"),
    ACTIVE("active", "活跃"),
    DEPRECATED("deprecated", "已弃用"),
    DISABLED("disabled", "已禁用");

    private final String code;
    private final String description;

    ModelStatus(String code, String description) {
        this.code = code;
        this.description = description;
    }
}

```text

## 8.3 API契约类型定义

### 8.3.1 OpenAPI规范定义

```yaml
# openapi/components/schemas.yaml
components:
  schemas:
    # 基础响应模型
    ApiResponse:
      type: object
      required:
        - code
        - message
        - data
        - timestamp
        - requestId
        - success
      properties:
        code:
          type: integer
          format: int32
          description: "响应码"
          example: 200
        message:
          type: string
          description: "响应消息"
          example: "操作成功"
        data:
          type: object
          description: "响应数据"
          nullable: true
        timestamp:
          type: integer
          format: int64
          description: "时间戳"
          example: 1704067200000
        requestId:
          type: string
          description: "请求ID"
          example: "req_123456789"
        success:
          type: boolean
          description: "是否成功"
          example: true

    # 分页响应模型
    PaginatedResponse:
      allOf:
        - $ref: '#/components/schemas/ApiResponse'
        - type: object
          properties:
            data:
              $ref: '#/components/schemas/PageResult'

    PageResult:
      type: object
      required:
        - items
        - total
        - page
        - pageSize
        - totalPages
        - hasNext
        - hasPrevious
      properties:
        items:
          type: array
          description: "数据列表"
        total:
          type: integer
          format: int64
          description: "总记录数"
          example: 100
        page:
          type: integer
          format: int32
          description: "当前页码"
          example: 1
        pageSize:
          type: integer
          format: int32
          description: "每页大小"
          example: 20
        totalPages:
          type: integer
          format: int32
          description: "总页数"
          example: 5
        hasNext:
          type: boolean
          description: "是否有下一页"
          example: true
        hasPrevious:
          type: boolean
          description: "是否有上一页"
          example: false

    # 错误响应模型
    ErrorResponse:
      type: object
      required:
        - code
        - message
        - path
        - timestamp
        - requestId
      properties:
        code:
          type: integer
          format: int32
          description: "错误码"
          example: 400
        message:
          type: string
          description: "错误消息"
          example: "请求参数无效"
        details:
          type: string
          description: "错误详情"
          nullable: true
        path:
          type: string
          description: "请求路径"
          example: "/api/v1/users"
        timestamp:
          type: integer
          format: int64
          description: "时间戳"
          example: 1704067200000
        requestId:
          type: string
          description: "请求ID"
          example: "req_123456789"
        errors:
          type: array
          description: "验证错误详情"
          items:
            $ref: '#/components/schemas/ValidationError'

    ValidationError:
      type: object
      required:
        - field
        - message
        - code
      properties:
        field:
          type: string
          description: "字段名"
          example: "email"
        message:
          type: string
          description: "错误消息"
          example: "邮箱格式不正确"
        code:
          type: string
          description: "错误码"
          example: "INVALID_EMAIL"
        rejectedValue:
          type: object
          description: "被拒绝的值"
          nullable: true

    # 用户相关模型
    UserDTO:
      type: object
      required:
        - id
        - username
        - email
        - status
        - createdAt
      properties:
        id:
          type: string
          format: uuid
          description: "用户ID"
          example: "123e4567-e89b-12d3-a456-426614174000"
        username:
          type: string
          description: "用户名"
          example: "john_doe"
        email:
          type: string
          format: email
          description: "邮箱"
          example: "john@example.com"
        phone:
          type: string
          description: "手机号"
          nullable: true
          example: "+8613812345678"
        status:
          type: string
          enum:
            - pending_activation
            - active
            - inactive
            - locked
            - suspended
          description: "用户状态"
          example: "active"
        profile:
          $ref: '#/components/schemas/UserProfileDTO'
        roles:
          type: array
          description: "用户角色"
          items:
            $ref: '#/components/schemas/RoleDTO'
        lastLoginAt:
          type: string
          format: date-time
          description: "最后登录时间"
          nullable: true
        loginCount:
          type: integer
          format: int32
          description: "登录次数"
          example: 42
        createdAt:
          type: string
          format: date-time
          description: "创建时间"
        updatedAt:
          type: string
          format: date-time
          description: "更新时间"

    UserProfileDTO:
      type: object
      properties:
        firstName:
          type: string
          description: "名"
          example: "John"
        lastName:
          type: string
          description: "姓"
          example: "Doe"
        displayName:
          type: string
          description: "显示名称"
          example: "John Doe"
        avatar:
          type: string
          description: "头像URL"
          nullable: true
        gender:
          type: string
          enum:
            - male
            - female
            - unknown
          description: "性别"
          example: "male"
        birthDate:
          type: string
          format: date
          description: "出生日期"
          nullable: true
        timezone:
          type: string
          description: "时区"
          example: "Asia/Shanghai"
        language:
          type: string
          description: "语言"
          example: "zh-CN"

    CreateUserRequest:
      type: object
      required:
        - username
        - email
        - password
      properties:
        username:
          type: string
          minLength: 3
          maxLength: 50
          pattern: '^[a-zA-Z0-9_]+$'
          description: "用户名"
          example: "john_doe"
        email:
          type: string
          format: email
          description: "邮箱"
          example: "john@example.com"
        password:
          type: string
          minLength: 8
          maxLength: 128
          description: "密码"
          example: "securePassword123"
        phone:
          type: string
          description: "手机号"
          nullable: true
        profile:
          $ref: '#/components/schemas/CreateUserProfileRequest'

    UpdateUserRequest:
      type: object
      properties:
        username:
          type: string
          minLength: 3
          maxLength: 50
          pattern: '^[a-zA-Z0-9_]+$'
          description: "用户名"
          nullable: true
        email:
          type: string
          format: email
          description: "邮箱"
          nullable: true
        phone:
          type: string
          description: "手机号"
          nullable: true
        profile:
          $ref: '#/components/schemas/UpdateUserProfileRequest'
        status:
          type: string
          enum:
            - active
            - inactive
            - locked
            - suspended
          description: "用户状态"
          nullable: true

    # AI模型相关模型
    AIModelDTO:
      type: object
      required:
        - id
        - name
        - type
        - provider
        - modelId
        - status
        - createdAt
      properties:
        id:
          type: string
          format: uuid
          description: "模型ID"
        name:
          type: string
          description: "模型名称"
          example: "GPT-4 Turbo"
        description:
          type: string
          description: "模型描述"
          nullable: true
        type:
          type: string
          enum:
            - text_generation
            - text_embedding
            - image_generation
            - code_generation
            - speech_recognition
            - translation
          description: "模型类型"
          example: "text_generation"
        provider:
          type: string
          enum:
            - openai
            - anthropic
            - google
            - azure
            - bedrock
            - local
          description: "模型提供商"
          example: "openai"
        modelId:
          type: string
          description: "提供商模型ID"
          example: "gpt-4-turbo-preview"
        version:
          type: string
          description: "模型版本"
          nullable: true
        parameters:
          type: object
          description: "模型参数"
          additionalProperties: true
        capabilities:
          type: object
          description: "模型能力"
          additionalProperties: true
        status:
          type: string
          enum:
            - development
            - testing
            - active
            - deprecated
            - disabled
          description: "模型状态"
          example: "active"
        costPerRequest:
          type: number
          format: double
          description: "每次请求成本"
          example: 0.002
        maxTokens:
          type: integer
          format: int32
          description: "最大token数"
          example: 4096
        rateLimit:
          type: integer
          format: int32
          description: "速率限制"
          example: 1000
        isDefault:
          type: boolean
          description: "是否默认模型"
          example: true
        createdAt:
          type: string
          format: date-time
          description: "创建时间"
        updatedAt:
          type: string
          format: date-time
          description: "更新时间"

```text

### 8.3.2 接口版本管理

```typescript
// types/api-versioning.d.ts
declare namespace IntelligentHub {
  // API版本信息
  interface APIVersion {
    version: string;
    status: 'stable' | 'beta' | 'deprecated' | 'sunset';
    releaseDate: Date;
    sunsetDate?: Date;
    changelog: string[];
  }

  // 版本化API响应
  interface VersionedResponse<T = any> extends BaseResponse<T> {
    apiVersion: string;
    deprecated?: boolean;
    sunsetDate?: Date;
    migrationGuide?: string;
  }

  // API兼容性配置
  interface APIConfig {
    currentVersion: string;
    supportedVersions: string[];
    defaultVersion: string;
    versioningStrategy: 'url' | 'header' | 'parameter';
    deprecationPolicy: {
      noticePeriod: number; // days
      sunsetPeriod: number; // days
    };
  }
}

```text

## 8.4 配置类型定义

### 8.4.1 环境配置类型

```java
// 应用配置
@ConfigurationProperties(prefix = "app")
@Data
public class ApplicationProperties {
    private String name;
    private String version;
    private String environment;
    private boolean debug;
    private Security security = new Security();
    private Cache cache = new Cache();
    private Database database = new Database();
    private Redis redis = new Redis();
    private Kafka kafka = new Kafka();
    private Ai ai = new Ai();
    private Monitoring monitoring = new Monitoring();

    @Data
    public static class Security {
        private Jwt jwt = new Jwt();
        private Cors cors = new Cors();
        private RateLimit rateLimit = new RateLimit();

        @Data
        public static class Jwt {
            private String secret;
            private Long expiration;
            private String issuer;
        }

        @Data
        public static class Cors {
            private String[] allowedOrigins = {"*"};
            private String[] allowedMethods = {"GET", "POST", "PUT", "DELETE", "OPTIONS"};
            private String[] allowedHeaders = {"*"};
            private Long maxAge = 3600L;
        }

        @Data
        public static class RateLimit {
            private boolean enabled = true;
            private int capacity = 100;
            private int refillRate = 10;
            private int refillDuration = 1;
        }
    }

    @Data
    public static class Cache {
        private Long defaultTtl = 3600L;
        private Redis redis = new Redis();

        @Data
        public static class Redis {
            private Long userTtl = 1800L;
            private Long permissionTtl = 3600L;
            private Long rateLimitTtl = 60L;
        }
    }

    @Data
    public static class Database {
        private String url;
        private String username;
        private String password;
        private String driverClassName;
        private Pool pool = new Pool();
        private Jpa jpa = new Jpa();

        @Data
        public static class Pool {
            private int initialSize = 5;
            private int maxActive = 20;
            private int minIdle = 5;
            private int maxIdle = 10;
            private Long maxWait = 60000L;
        }

        @Data
        public static class Jpa {
            private String dialect;
            private boolean showSql = false;
            private boolean formatSql = true;
            private DdlAuto ddlAuto = DdlAuto.NONE;
            private Properties properties = new Properties();
        }
    }

    @Data
    public static class Redis {
        private String host = "localhost";
        private int port = 6379;
        private String password;
        private int database = 0;
        private Long timeout = 2000L;
        private Pool pool = new Pool();

        @Data
        public static class Pool {
            private int maxActive = 20;
            private int maxIdle = 10;
            private int minIdle = 5;
            private Long maxWait = 3000L;
        }
    }

    @Data
    public static class Kafka {
        private String bootstrapServers = "localhost:9092";
        private Producer producer = new Producer();
        private Consumer consumer = new Consumer();

        @Data
        public static class Producer {
            private String acks = "all";
            private int retries = 3;
            private int batchSize = 16384;
            private Long lingerMs = 1L;
            private Long bufferMemory = 33554432L;
        }

        @Data
        public static class Consumer {
            private String groupId;
            private String autoOffsetReset = "earliest";
            private boolean enableAutoCommit = false;
            private int concurrency = 1;
        }
    }

    @Data
    public static class Ai {
        private OpenAi openAi = new OpenAi();
        private Anthropic anthropic = new Anthropic();
        private AzureOpenAi azureOpenAi = new AzureOpenAi();

        @Data
        public static class OpenAi {
            private String apiKey;
            private String baseUrl = "https://api.openai.com/v1";
            private Long timeout = 30000L;
            private int maxRetries = 3;
        }

        @Data
        public static class Anthropic {
            private String apiKey;
            private String baseUrl = "https://api.anthropic.com";
            private Long timeout = 30000L;
            private int maxRetries = 3;
        }

        @Data
        public static class AzureOpenAi {
            private String apiKey;
            private String endpoint;
            private String apiVersion = "2023-12-01-preview";
            private String deploymentName;
        }
    }

    @Data
    public static class Monitoring {
        private Prometheus prometheus = new Prometheus();
        private Metrics metrics = new Metrics();
        private Tracing tracing = new Tracing();

        @Data
        public static class Prometheus {
            private boolean enabled = true;
            private String path = "/actuator/prometheus";
        }

        @Data
        public static class Metrics {
            private boolean enabled = true;
            private Export export = new Export();

            @Data
            public static class Export {
                private boolean enabled = true;
                private Long step = 60000L;
            }
        }

        @Data
        public static class Tracing {
            private boolean enabled = true;
            private String exporter = "jaeger";
            private Double samplingProbability = 0.1;
        }
    }
}

```text

### 8.4.2 特性开关定义

```java
// 特性开关配置
@ConfigurationProperties(prefix = "features")
@Data
public class FeatureFlags {
    private AiFeatures ai = new AiFeatures();
    private SecurityFeatures security = new SecurityFeatures();
    private ExperimentalFeatures experimental = new ExperimentalFeatures();

    @Data
    public static class AiFeatures {
        private boolean textGeneration = true;
        private boolean imageGeneration = true;
        private boolean codeGeneration = true;
        private boolean speechRecognition = false;
        private boolean translation = true;
        private boolean fineTuning = false;
        private boolean ragEnabled = true;
    }

    @Data
    public static class SecurityFeatures {
        private boolean twoFactorAuth = true;
        private boolean rateLimiting = true;
        private boolean auditLogging = true;
        private boolean apiKeyAuthentication = true;
        private boolean ssoIntegration = false;
    }

    @Data
    public static class ExperimentalFeatures {
        private boolean virtualThreads = true;
        private boolean nativeImage = false;
        private boolean vectorSearch = true;
        private boolean graphQl = false;
        private boolean webFlux = false;
    }
}

```text

## 8.5 消息类型定义

### 8.5.1 事件类型规范

```java
// 基础事件类
public abstract class BaseDomainEvent implements DomainEvent {
    private final String eventId;
    private final Date occurredOn;
    private final String aggregateId;
    private final String aggregateType;

    protected BaseDomainEvent(String aggregateId, String aggregateType) {
        this.eventId = UUID.randomUUID().toString();
        this.occurredOn = new Date();
        this.aggregateId = aggregateId;
        this.aggregateType = aggregateType;
    }

    @Override
    public String getEventId() {
        return eventId;
    }

    @Override
    public Date getOccurredOn() {
        return occurredOn;
    }

    @Override
    public String getAggregateId() {
        return aggregateId;
    }

    @Override
    public String getAggregateType() {
        return aggregateType;
    }

    @Override
    public abstract String getEventType();
}

// 用户相关事件
public class UserCreatedEvent extends BaseDomainEvent {
    private final String username;
    private final String email;
    private final UserStatus status;

    public UserCreatedEvent(String aggregateId, String username, String email, UserStatus status) {
        super(aggregateId, "User");
        this.username = username;
        this.email = email;
        this.status = status;
    }

    @Override
    public String getEventType() {
        return "USER_CREATED";
    }

    // getters...
}

public class UserPasswordChangedEvent extends BaseDomainEvent {
    private final String newPasswordHash;

    public UserPasswordChangedEvent(String aggregateId, String newPasswordHash) {
        super(aggregateId, "User");
        this.newPasswordHash = newPasswordHash;
    }

    @Override
    public String getEventType() {
        return "USER_PASSWORD_CHANGED";
    }

    // getters...
}

public class UserRoleAssignedEvent extends BaseDomainEvent {
    private final String roleId;

    public UserRoleAssignedEvent(String aggregateId, String roleId) {
        super(aggregateId, "User");
        this.roleId = roleId;
    }

    @Override
    public String getEventType() {
        return "USER_ROLE_ASSIGNED";
    }

    // getters...
}

// AI模型相关事件
public class ModelRegisteredEvent extends BaseDomainEvent {
    private final String modelName;
    private final ModelType modelType;
    private final ModelProvider provider;

    public ModelRegisteredEvent(String aggregateId, String modelName, 
                               ModelType modelType, ModelProvider provider) {
        super(aggregateId, "AIModel");
        this.modelName = modelName;
        this.modelType = modelType;
        this.provider = provider;
    }

    @Override
    public String getEventType() {
        return "MODEL_REGISTERED";
    }

    // getters...
}

public class ModelParametersUpdatedEvent extends BaseDomainEvent {
    private final Map<String, Object> parameters;

    public ModelParametersUpdatedEvent(String aggregateId, Map<String, Object> parameters) {
        super(aggregateId, "AIModel");
        this.parameters = parameters;
    }

    @Override
    public String getEventType() {
        return "MODEL_PARAMETERS_UPDATED";
    }

    // getters...
}

```text

### 8.5.2 命令类型定义

```java
// 基础命令接口
public interface Command<T> {
    String getCommandId();
    Date getTimestamp();
    String getCommandType();
    T getPayload();
}

// 用户相关命令
public class CreateUserCommand implements Command<CreateUserCommand.Payload> {
    private final String commandId;
    private final Date timestamp;
    private final Payload payload;

    public CreateUserCommand(String username, String email, String password, 
                            String phone, UserProfile profile) {
        this.commandId = UUID.randomUUID().toString();
        this.timestamp = new Date();
        this.payload = new Payload(username, email, password, phone, profile);
    }

    @Override
    public String getCommandId() {
        return commandId;
    }

    @Override
    public Date getTimestamp() {
        return timestamp;
    }

    @Override
    public String getCommandType() {
        return "CREATE_USER";
    }

    @Override
    public Payload getPayload() {
        return payload;
    }

    @Data
    @AllArgsConstructor
    public static class Payload {
        private String username;
        private String email;
        private String password;
        private String phone;
        private UserProfile profile;
    }
}

public class ChangeUserPasswordCommand implements Command<ChangeUserPasswordCommand.Payload> {
    private final String commandId;
    private final Date timestamp;
    private final Payload payload;

    public ChangeUserPasswordCommand(String userId, String oldPassword, String newPassword) {
        this.commandId = UUID.randomUUID().toString();
        this.timestamp = new Date();
        this.payload = new Payload(userId, oldPassword, newPassword);
    }

    @Override
    public String getCommandId() {
        return commandId;
    }

    @Override
    public Date getTimestamp() {
        return timestamp;
    }

    @Override
    public String getCommandType() {
        return "CHANGE_USER_PASSWORD";
    }

    @Override
    public Payload getPayload() {
        return payload;
    }

    @Data
    @AllArgsConstructor
    public static class Payload {
        private String userId;
        private String oldPassword;
        private String newPassword;
    }
}

// AI模型相关命令
public class RegisterModelCommand implements Command<RegisterModelCommand.Payload> {
    private final String commandId;
    private final Date timestamp;
    private final Payload payload;

    public RegisterModelCommand(String name, String description, ModelType type, 
                              ModelProvider provider, String modelId, 
                              Map<String, Object> parameters) {
        this.commandId = UUID.randomUUID().toString();
        this.timestamp = new Date();
        this.payload = new Payload(name, description, type, provider, modelId, parameters);
    }

    @Override
    public String getCommandId() {
        return commandId;
    }

    @Override
    public Date getTimestamp() {
        return timestamp;
    }

    @Override
    public String getCommandType() {
        return "REGISTER_MODEL";
    }

    @Override
    public Payload getPayload() {
        return payload;
    }

    @Data
    @AllArgsConstructor
    public static class Payload {
        private String name;
        private String description;
        private ModelType type;
        private ModelProvider provider;
        private String modelId;
        private Map<String, Object> parameters;
    }
}

```text

### 8.5.3 集成事件类型

```java
// 集成事件基类
public abstract class IntegrationEvent {
    private final String eventId;
    private final Date occurredOn;
    private final String eventType;
    private final String source;

    protected IntegrationEvent(String eventType, String source) {
        this.eventId = UUID.randomUUID().toString();
        this.occurredOn = new Date();
        this.eventType = eventType;
        this.source = source;
    }

    // getters...
}

// 系统集成事件
public class UserSyncEvent extends IntegrationEvent {
    private final String userId;
    private final String action; // CREATE, UPDATE, DELETE
    private final UserDTO userData;

    public UserSyncEvent(String userId, String action, UserDTO userData) {
        super("USER_SYNC", "intelligent-hub");
        this.userId = userId;
        this.action = action;
        this.userData = userData;
    }

    // getters...
}

public class ModelUsageEvent extends IntegrationEvent {
    private final String modelId;
    private final String userId;
    private final String operation;
    private final Long tokensUsed;
    private final Double cost;
    private final Long duration;

    public ModelUsageEvent(String modelId, String userId, String operation, 
                          Long tokensUsed, Double cost, Long duration) {
        super("MODEL_USAGE", "intelligent-hub");
        this.modelId = modelId;
        this.userId = userId;
        this.operation = operation;
        this.tokensUsed = tokensUsed;
        this.cost = cost;
        this.duration = duration;
    }

    // getters...
}

```text

---
